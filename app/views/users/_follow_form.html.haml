.follow_form
  - if current_user.following? @user
    = link_to "フォローを外す", {controller: :users,action: :unfollow,user_id: @user.id}, class: "unfollow__btn", method: :put
  - else
    = link_to "フォローする", {controller: :users,action: :follow,user_id: @user.id}, class: "follow__btn", method: :put
